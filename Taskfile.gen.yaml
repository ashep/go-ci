version: "3"

tasks:
  default:
    desc: Run all code generation tools
    cmds:
      - task: proto

  proto:
    desc: Generate protobuf code
    cmds:
      - rm -rvf ./sdk/proto
      - buf lint proto
      - buf generate
