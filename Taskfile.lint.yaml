version: '3'

vars:
  APP_NAME:
    sh: basename $(pwd)

tasks:
  default:
    desc: Run all linters
    cmds:
      - task: golangci

  golangci:
    desc: Run 'golangci-lint' linter
    cmds:
      - golangci-lint run ./...